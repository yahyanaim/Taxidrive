# Example connection string for local development.
#
# Create a Postgres database and update credentials/host/port/db name as needed.
#
# Prisma expects DATABASE_URL to point to the database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/app?schema=public"
